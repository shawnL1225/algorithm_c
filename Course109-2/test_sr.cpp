#include <bits/stdc++.h>
using namespace std;

typedef long long lint;
int main(){
    ios_base::sync_with_stdio(0); cin.tie(0);
    int n;
    while(n--){
        int x,y,p,q;
        cin >> x >> y >> p >> q;
        int div = q-p;
        int de = y*p - x*q;
        int ans = 
    }


    return 0;
}